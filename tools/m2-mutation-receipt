# Mutation testing receipt (SPEC.md §11.2, D-088 amendment)
# Written by: python3 tools/mutation-test.py (full, non-chunked run)
# Updated: bump this file after every clean full-suite run.
# Format: one line per domain "domain=killed/total" (total includes documented
# equivalent mutants), final "total=" line parsed by prumo-verify.
# SHA must be HEAD or an ancestor; prumo-verify gates on it.
# v0.5.0 closeout run 2026-08-22 09:27 UTC: adds d089 CHECK-ABSENCE mutants
# (MD3 flock -> grant_ledger, MD4 compact -> intent, MD5 dedup -> relay).
# v0.5.3 closeout run 2026-08-24: adds the d090 audit-path defence domain;
# md5-dedup SKIP caught (stale anchor after fmt canonicalization), anchor
# repaired, chunk re-run 3/3. Combined 168/168 + 1 EQUIV at 7035eed.
# v0.6.0 closeout run 2026-08-24 (~12:20 UTC): adds the d091 control-plane
# defence domain. Full run at d4ebf81: 174/174 non-equivalent killed,
# 0 survived, 0 TIMEOUT (fence armed but never fired), 1 documented
# equivalent. grant check 7 remains the sole equivalent.
# v0.6.1 closeout runs 2026-08-24 (~13:20-15:10 UTC): adds the d092 pre-audit
# defence domain (F15 cert-v3 -> ca_material.zig, F16 subject-binding ->
# control_api.zig). Legacy leg: FULL at 90e46a5, 174 KILLED + 1 EQUIV
# (175 evaluated), 0 survived, 0 TIMEOUT. First attempt then crashed in the
# harness (KeyError: ca_material.zig missing from TARGETS) when reaching the
# first d092 mutant - registration bug, all legacy results unaffected. Fixed
# in 322622c (tools-only, zero src/ drift verified by git diff), d092 chunk
# re-run with MUTATION_DOMAIN=d092: 2/2 KILLED. Combined at 322622c:
# 176/176 non-equivalent killed, 0 survived, 1 documented equivalent,
# 177 evaluated.
# afe5fe4 full re-run 2026-08-25: 176/176 non-equivalent killed, 0 survived, 1 documented equivalent, 0 TIMEOUT (M1 bind slice BE-CTRL-03 + BE-TR-01a)
# f6eb3ac full re-run 2026-08-26 (~08:06-09:0x UTC, cron bolina-continuity): drift after
# the SPEC 4.1a type-2 conformant fix (e4fd0d4 touched src/handshake.zig + src/pilot_test.zig).
# Clean full suite: 176/176 non-equivalent killed, 0 survived, 0 SKIP, 0 TIMEOUT,
# 1 documented equivalent. Per-domain tally identical to the afe5fe4 leg.
sha=f6eb3ac
channel=8/8
d089=3/3
d090=6/6
d091=6/6
d092=2/2
daemon=7/7
dispatch=6/6
evidence=11/11
grant=22/22 (1 documented equivalent: BE-GRANT-03 check 7, see EQUIVALENT in tools/mutation-test.py)
grant_ledger=7/7
grant_revocation=4/4
intent=6/6
ledger=13/13
mesh=13/13
refusal=3/3
relay=11/11
relay_serve=6/6
render=6/6
resolver=10/10
session=11/11
sync=12/12
transport=4/4
total=176/176 survived=0 equivalent=1
